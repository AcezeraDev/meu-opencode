Take a screenshot of the current page of the built-in browser and attach it as an
image.

Only useful when you can actually look at images, and when the visual result is
what matters: layout, styling, charts, or checking that a page renders correctly.
For reading content or finding elements to click, browser_snapshot is far cheaper
and more precise.

Pass `ref` or `selector` to capture a single element, or `fullPage` to capture the
entire scrollable page instead of just the viewport.

Pass `marks: true` to have every element you can act on drawn as a numbered box:
the number is its ref (12 is ref_12), usable right away with browser_act. The
result lists the boxes with their names too. Useful when the outline alone does
not say which button is which: icons, colors, drag-and-drop, a crowded layout.

The result also lists the text the screenshot shows, top to bottom, so it can be
read even without seeing the image. Without marks, acting on the page still needs
refs from browser_snapshot.
